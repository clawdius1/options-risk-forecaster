# No secrets required for the free-data path (yfinance + AlphaQuery).
# If you later add a paid historical options feed, put keys here and
# load them from the environment — never commit real values.

# POLYGON_API_KEY=
# THETADATA_USERNAME=
# THETADATA_PASSWORD=
# ORATS_API_KEY=
# TRADIER_TOKEN=
